server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/LoanSolicitudeServiceDB
    username: postgres
    password: Silvestre9+
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/